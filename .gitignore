# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp

# Cache clone repos (diisi saat /updateskills, tidak perlu di-commit)
skills-cache/

# Local session state — ephemeral, tidak perlu di-repo
prompt_counter.txt
.gstack/

# QA screenshot evidence (besar, temporary per testing session)
qa_orms_screenshots/
